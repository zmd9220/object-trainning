#include <iostream>
#include <algorithm>
using namespace std;

int main()
{
	std::ios::sync_with_stdio(false);
	int N, T, M;
	cin >> T;
	for (int i = 0; i < T; i++)
	{
		cin >> N >> M;
		for (int j = 0; j < N; j++)
		{
			
		}
	}
	return 0;
}